#define __OS__
use "lib/std.kaba"


void _mkdir(string filename)
	int r = mkdir(filename)
	if r < 0
		print("mkdir: kann Verzeichnis nicht anlegen: " + filename + "\n")
		exit(-1)


void main()
	init_lib()
	string[] arg
	get_args(arg)
	
	for i in 1:arg.num
		_mkdir(arg[i])
	
	exit(0)
