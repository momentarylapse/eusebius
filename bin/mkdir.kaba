use lib.std


void _mkdir(string filename)
	if !mkdir(filename)
		print("mkdir: can not create directory: " + filename)
		exit(lib_error)


void main()
	init_lib()
	string[] arg
	get_args(arg)
	
	for i in 1:arg.num
		_mkdir(arg[i])
	
	exit(0)
