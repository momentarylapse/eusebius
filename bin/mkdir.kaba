use "lib/std.kaba"


void _mkdir(string filename)
	if !mkdir(filename)
		print("mkdir: kann Verzeichnis nicht anlegen: " + filename + "\n")
		exit(lib_error)


void main()
	init_lib()
	string[] arg
	get_args(arg)
	
	for i in 1:arg.num
		_mkdir(arg[i])
	
	exit(0)
