use lib.std



void main()
	init_lib()
	lib_print_endl = false
	string[] arg
	get_args(arg)
	
	while true
		string s
		s.resize(128)
		int r = read(stdin, &s[0], s.num)
		if r <= 0
			break
		s.resize(r)
		print("<")
		print(s)
		print(">")
	
	exit(0)
