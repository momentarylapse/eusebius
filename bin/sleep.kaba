#define __OS__
use "prog_header.kaba"



void main()
	init_lib()
	string[] arg
	get_args(arg)
	
	if arg.num == 2
		int n = arg[1].int()
		TimeValue t, more
		t.sec = n
		t.nanosec = 0
		nanosleep(t, more)
	
	exit(0)
