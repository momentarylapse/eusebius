#define __STRING_CONST_AS_CSTRING__

use common
use io.text

func main()

	TextMode.init()
	TextMode.clear()
	
	prints("hello 64bit:  ")
	let a = 0x0000000100000000
	let b = 0x0000000200000000
	let c = a + b
	let d = int(c >> 32)
	
	printi(d)


	while true
		pass

